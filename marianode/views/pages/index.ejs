<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="css/style.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM" crossorigin="anonymous"></script>
    <!-- Option 1: Include in HTML -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <script src="js/default.js"></script>
</head>
<body class="container-fluid">
    <br>
    <h3>Bootstrap 5.0 + ejs + fetch(frontend) + nodejs + expressjs + mariadb (backend)</h3>
    <% if(notification != null){%>
        <div class="alert alert-info">
            <%= notification %>
        </div>
    <% } %>
    <table class="table table-striped">
        <thead class="table-light">
            <tr>
                <td>
                    <button type="button" onclick="refreshMe()" class="btn btn-primary">
                        <i class="bi bi-tropical-storm"></i>
                    </button>
                </td>
                <td>
                    <input type="text" class="form-control" id="name" placeholder="Name"/>
                </td>
                <td>
                    <input type="text" class="form-control" id="age" placeholder="Age"/>
                </td>
                <td>
                    <button type="button" onclick="createRecord()" class="btn btn-success">
                        <i class="bi bi-plus-circle"></i>
                    </button>
                </td>
            </tr>
            <tr>
                <th>Person Id</th>
                <th>Name</th>
                <th>Age</th>
                <th>Action</th>
            </tr>
        </thead>
        <tbody>
            <% data.forEach(element=>{ %>
                <tr>
                    <td>
                        <%= element.id %>
                    </td>
                    <td>
                        <input type="text" class="form-control" id="<%= element.id %>_name" value="<%= element.name %>">
                    </td>
                    <td>
                        <input type="text" class="form-control" id="<%= element.id %>_name" value="<%= element.age %>">
                    </td>
                    <td>
                        <div class="btn-group">
                            <button type="button" onclick="updateRecord('<%= element.id %>')" class="btn btn-warning">
                                <i class="bi bi-file-diff"></i>
                            </button>
                            <button type="button" onclick="deleteRecord('<%= element.id %>')" class="btn btn-danger">
                                <i class="bi bi-trash"></i>
                            </button>
                        </div>
                    </td>
                </tr>
            <% }); %>
        </tbody>
    </table>

    <script>
  
    </script>
</body>
</html>